{% extends 'base.html.twig' %}

{% block title %}Birthday{% endblock %}

{% block body %}
    <h1>Birthday</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ birthday.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ birthday.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ birthday.lastname }}</td>
            </tr>
            <tr>
                <th>Birthdate</th>
                <td>{{ birthday.birthdate ? birthday.birthdate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_birthday_index') }}">back to list</a>

    <a href="{{ path('app_birthday_edit', {'id': birthday.id}) }}">edit</a>

    {{ include('birthday/_delete_form.html.twig') }}
{% endblock %}
